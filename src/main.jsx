import React from 'react';
import ReactDOM from 'react-dom/client';
import GovSignal from './govcon-tracker.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GovSignal />
  </React.StrictMode>
);